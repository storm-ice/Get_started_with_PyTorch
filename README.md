# Get_started_with_PyTorch

```bash
git init
git remote add origin https://github.com/storm-ice/Get_started_with_PyTorch
git remote -v
git add .
git status
git commit -m "first"
git push -u origin main
```

